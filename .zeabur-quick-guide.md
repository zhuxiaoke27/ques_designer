# Zeabur 快速部署参考

## 📦 部署清单

### 后端服务（backend）

**检测类型**：Python
**启动方式**：Procfile → `gunicorn app:app`

**必需文件：**
- ✅ `requirements.txt` - Python 依赖
- ✅ `Procfile` - 启动配置
- ✅ `runtime.txt` - Python 版本
- ✅ `app.py` - Flask 应用入口

**环境变量：**
```
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.v3.cm/v1/
CORS_ORIGINS=https://your-frontend.zeabur.app
DEBUG=False
```

**自动提供的变量：**
- `PORT` - Zeabur 自动分配

---

### 前端服务（frontend）

**检测类型**：Node.js → Static Site
**构建命令**：`npm run build`
**输出目录**：`dist/`

**必需文件：**
- ✅ `package.json` - 项目配置
- ✅ `vite.config.js` - 构建配置

**环境变量：**
```
VITE_API_BASE_URL=https://your-backend.zeabur.app/api
```

---

## 🚀 部署顺序

1. **先部署后端** → 获取后端 URL
2. **再部署前端** → 配置后端 URL
3. **更新后端 CORS** → 添加前端域名

---

## ✅ 验证步骤

1. 后端健康检查：`https://backend-xxx.zeabur.app/api/health`
2. 前端访问测试：`https://frontend-xxx.zeabur.app`
3. 功能测试：生成一份问卷

---

## 🔧 常见配置

### Zeabur 服务设置

**后端：**
- Service Name: `backend`
- Root Directory: `backend/`
- Build Command: 自动检测
- Start Command: 来自 Procfile

**前端：**
- Service Name: `frontend`
- Root Directory: `frontend/`
- Build Command: `npm run build`
- Output Directory: `dist/`

---

## 📝 注意事项

1. ⚠️ 首次部署后端需要 3-5 分钟初始化向量数据库
2. ⚠️ 环境变量修改后需要重启服务
3. ⚠️ CORS 配置必须与前端域名匹配
4. ⚠️ API 密钥需要妥善保管

---

详细文档：[DEPLOYMENT.md](./DEPLOYMENT.md)
